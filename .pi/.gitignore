# OS junk
.DS_Store

# Dependencies
node_modules/

# pi-managed package installs (regenerated from settings.json on trusted
# startup; the git/ clone carries its own nested .git, so tracking it would
# create a broken gitlink).
git/
npm/

# Environment
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/

# Runtime state
.memory/
*.log
